This is a demo app that displays photos using this api: http://jsonplaceholder.typicode.com/photos

Features:
- adaptive to all iphones, ipads and orientations
- implemented as whole in Swift or Objective-C. Xcode schemes will launch respective implementation
- responsive
- object graph looks like rootVC = navigationVC -> LandingVC -> SplitVC -> AlbumsVC + PhotosVC -> PhotoVC (saving image)
- does not use third party software
- is online/offline capable
